# macOS
.DS_Store

# Xcode
xcuserdata/
DerivedData/
build/
*.ipa
*.dSYM
*.dSYM.zip
timeline.xctimeline

# SwiftPM
.build/
.swiftpm/

# The model pipeline now lives in https://github.com/OCRadar/Model.
# These remain so a local clone of it, or a stray export, cannot be committed
# here by accident — model artifacts belong in the app bundle, not in git.
__pycache__/
*.pyc
*.egg-info/
*.pt
*.pth
